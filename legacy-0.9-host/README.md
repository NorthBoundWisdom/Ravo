# Frozen 0.9 host build files

`CMakeLists.txt` here is the former repository-root Darktable 0.9 build graph.
It is kept only as a reference snapshot. Do not configure, compile, or test it.

The former `src/` tree now lives in `../legacy-0.9/` and is also reference-only.
The live CMake project is the repository-root `CMakeLists.txt`, which builds Ravo.
