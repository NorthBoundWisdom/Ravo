# Frozen 0.9 host build files

This directory holds the former repository-root Darktable 0.9 CMake graph,
`cmake/` modules, `data/` resources, packaging, and host tools. Do not
configure, compile, or test it.

Application sources are in `../src/`. Live CMake is the repository-root
`CMakeLists.txt`, which builds only Ravo.
